# Plot Index — Phases 2–7 (Rajasthan)

Generated by `src/plots/make_plots.py`:
```
python pipeline.py --state rajasthan --stage plots
# or: python -m src.plots.make_plots rajasthan
```
Needs `plotly` + `kaleido` (already installed). Every figure is saved
twice under `results/plots/`:
- `interactive/<name>.html` — **open this one first.** Fully
  self-contained (no internet needed), opens in any browser by
  double-clicking. Hover for exact values, drag to zoom, click legend
  entries to toggle. Git-ignored (large — inlined plotly.js).
- `static/<name>.png` — the same figure as a flat image, for a report,
  slide deck, or printed viva material. Committed.

Nothing in these plots is a mockup — each is generated from files Phases
2–7 already produced (`phase5_design_cases.parquet`,
`phase7_optimized_designs.csv`, `phase6_surrogate_models.pkl`, …), or
from re-running one specific already-verified case through the real
simulator to get a time series. Re-run `--stage plots` after any phase
changes to regenerate every figure from the current results.

`src/plots/make_plots.py` is ported from `objective2-tamilnadu/`; the
figure code is unchanged, only the flat `results/phaseN_*` input paths,
the 3-regime subplot grids (TN has 5), Rajasthan's PCM names, and the
title-string case counts differ.

## 15 figures

| File | Covers |
|---|---|
| `02_geometry_plots.md` | Phase 2 — design-space validity map, Ergun hydraulics curve |
| `03_simulator_plots.md` | Phase 3 — temperature / melt-fraction time series, energy breakdown |
| `04_verification_plots.md` | Phase 4 — Gate 1 residuals, Gate 3 baseline comparison, Gate 5 sensitivity |
| `05_doe_plots.md` | Phase 5 — DOE sample coverage, outcome distribution |
| `06_surrogate_plots.md` | Phase 6 — parity plots, feature importance |
| `07_optimization_plots.md` | Phase 7 — Pareto view, surrogate-vs-simulator, safety compliance |

## One-line justification per phase (for a viva or report caption)

- **Phase 2**: "the geometry engine's validity boundary is exactly where
  the math predicts it (diameter = 0.04 m), not an arbitrary cutoff —
  and it explains the 54/165 ≈ 32.7% DOE rejection rate."
- **Phase 3**: "the simulator produces physically plausible daily
  heating / cooling cycles and a visible PCM melting plateau at RT50's
  48 °C melting point, not noise."
- **Phase 4**: "energy is conserved to ~10⁻³–10⁻⁴ %, and every
  sensitivity check moves in the physically correct direction; unlike
  Tamil Nadu, RT50 here *slightly* beats the plain tank on solar
  fraction."
- **Phase 5**: "the DOE covers the full design space and produces a
  smooth, sensible spread of outcomes; the rejections are all one
  deterministic geometric rule, not scattered bad luck."
- **Phase 6**: "the surrogate's predictions sit almost exactly on the
  parity line — R² ≈ 1.0 is visually obvious, not just a table number."
- **Phase 7**: "the surrogate and the real simulator agree to 0.03 % on
  60 designs it wasn't directly trained on; the safety-compliance bar
  shows *why* the optimizer picked the plain tank in every regime —
  0/45 PCM candidates clear the 65 °C limit, 15/15 plain-tank ones do."
