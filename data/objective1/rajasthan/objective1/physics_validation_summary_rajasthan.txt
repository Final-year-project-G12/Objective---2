====================================================================
  PHASE 7 PHYSICS VALIDATION SUMMARY — Rajasthan
====================================================================

[DATA PROVENANCE] Cross-file fingerprint check PASSED — feasibility_survivors_rajasthan_kappa_calibrated.csv (Phase 5) and mcdm_rankings_rajasthan.csv (Phase 6) were both confirmed built from the SAME on-disk cluster_profiles_rajasthan.csv (this script hard-fails before reaching this point otherwise — see provenance_lib.py). An earlier run (2026-08-11) caught these two files disagreeing cluster-by-cluster on (cluster_id, pcm_id) pairing due to GMM cluster-index relabeling between separate re-runs of 05_cluster_rajasthan.py — fixed via canonical relabeling (by ascending mean latitude) in that script plus this hard-fail check; that invalid run's rho values (-0.90/-0.10/-0.11) were NOT the numbers being reported here.

Calibration: 100% of medoids landed in the 54-84% published solar-fraction benchmark band (target ~69%) using the calibration PCM (RT47). PCM-vs-plain-tank comparator: -0.0% (framework doc cites +30%/+4-8% from literature — see physics_lib.py's CALIBRATION section for why this specific comparator landed far below that range here, honestly reported rather than tuned away).
PCM-mass sensitivity sweep (50-800 kg, 5 representative candidates): solar-fraction spread widens with mass (True); ranking of these 5 candidates stable across all masses tested (True). This means the small (<1pp) spread at the pipeline-default 50kg PCM sizing is a real, low-amplitude signal rather than noise, and the negative rho below is not an artifact of insufficient differentiation at that sizing.

--- Cluster 0: NEGATIVE (rho<=0.4) ---
CLUSTER 0 (n=9 candidates, medoid=RJP_0132): Spearman rho (Borda vs. simulated solar fraction) = -0.385 (p=0.306). Borda and Copeland disagreed on Top-3 membership here (Phase 6's own flagged case) — Copeland-vs-simulation rho = -0.402, reported alongside Borda rather than picking one silently. CAUTION: this cluster's own MCDM ranking is flagged by Phase 6 as below the 0.6 ambiguous-agreement threshold (W=0.3875). Any low rho here should be read as EITHER a genuine physics/MCDM mismatch OR the MCDM ranking's own pre-existing instability (an unstable input has no stable target to correlate against) — these are different diagnoses requiring different fixes (re-weighting criteria vs. expanding the candidate pool / re-running Phase 5-6), and this script does not have enough information to pick between them on its own. GENUINE NEGATIVE RESULT (rho<=0.4): MCDM inputs/weights need diagnosis for this cluster, not a discarded run. Dominant entropy-weighted criterion: 'supercooling' (63.8%) — the criterion most likely responsible for the divergence, based on where the two rankings disagree most. For Cluster 0 specifically: given kendalls_w_cluster=0.3875 (below the 0.6 ambiguous threshold) and candidate_pool_status='healthy' (n=9<8), this is BETTER EXPLAINED by the MCDM ranking's OWN pre-existing instability than by a genuine physics disagreement — the four MCDM methods did not agree with EACH OTHER here either, so a low correlation against any single external target (including this simulation) is the expected consequence of an unstable input, not new evidence against the MCDM weights specifically. The fix indicated is expanding this cluster's candidate pool (currently n=9), not re-weighting criteria. Phase 6 caveats carried forward: every candidate in this cluster is tagged 'COMPLETE — 55-row manufacturer database (+7 literature rows = 62 candidates total) as of 2026-08-12, inside the 40-60-row target (was ~25 rows/18 manufacturer at the prior run)'; cost is always NaN and corrosion is a binary structural proxy in the underlying data, not measured properties this simulation can independently verify either.

--- Cluster 1: NEGATIVE (rho<=0.4) ---
CLUSTER 1 (n=14 candidates, medoid=RJP_0202): Spearman rho (Borda vs. simulated solar fraction) = 0.125 (p=0.670). GENUINE NEGATIVE RESULT (rho<=0.4): MCDM inputs/weights need diagnosis for this cluster, not a discarded run. Dominant entropy-weighted criterion: 'supercooling' (48.6%) — the criterion most likely responsible for the divergence, based on where the two rankings disagree most. Phase 6 caveats carried forward: every candidate in this cluster is tagged 'COMPLETE — 55-row manufacturer database (+7 literature rows = 62 candidates total) as of 2026-08-12, inside the 40-60-row target (was ~25 rows/18 manufacturer at the prior run)'; cost is always NaN and corrosion is a binary structural proxy in the underlying data, not measured properties this simulation can independently verify either.

--- Cluster 2: NEGATIVE (rho<=0.4) ---
CLUSTER 2 (n=16 candidates, medoid=RJP_0055): Spearman rho (Borda vs. simulated solar fraction) = -0.097 (p=0.720). GENUINE NEGATIVE RESULT (rho<=0.4): MCDM inputs/weights need diagnosis for this cluster, not a discarded run. Dominant entropy-weighted criterion: 'supercooling' (57.0%) — the criterion most likely responsible for the divergence, based on where the two rankings disagree most. Phase 6 caveats carried forward: every candidate in this cluster is tagged 'COMPLETE — 55-row manufacturer database (+7 literature rows = 62 candidates total) as of 2026-08-12, inside the 40-60-row target (was ~25 rows/18 manufacturer at the prior run)'; cost is always NaN and corrosion is a binary structural proxy in the underlying data, not measured properties this simulation can independently verify either.

====================================================================